{
  "name": "fitness-checkin",
  "version": "1.0.0",
  "description": "健身打卡 — 极简本地健身打卡 App (PWA wrapped as Android APK via Capacitor)",
  "private": true,
  "scripts": {
    "build:android": "npx cap sync android && npx cap build android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
