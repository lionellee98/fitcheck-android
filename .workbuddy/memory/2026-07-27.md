# 2026-07-27

## 将健身动作数据集封装为 WorkBuddy 技能
- 用户提供一个 zip（`exercises-dataset-zh-master.zip`），本以为是技能包，实际是健身动作数据集（hasaneyldrm/exercises-dataset 的中文增强版）：1,324 个动作 + 演示 GIF（127MB）+ 缩略图（12MB）+ 17MB JSON。
- 用户选择「封装成 WorkBuddy 技能」。已创建用户级技能：`~/.workbuddy/skills/exercise-dataset-zh/`
  - SKILL.md（frontmatter 含 agent_created:true）
  - scripts/lookup.py：自定位路径，支持 search / get / filter / random / plan 子命令（仅用 Python 标准库）
  - data/（exercises.json, name_zh.json, exercises.schema.json）、assets/images/、assets/videos/
- 安全审计（手动，因 skills-security-check 技能在本环境不可用）：P2 安全。脚本仅读取本地 JSON/媒体，无网络/无写入/无命令注入。
- 冒烟测试通过：中文搜索「卧推」、filter（chest+dumbbell）、plan（3天增肌）均正常，GIF 路径可解析。
- 后续：技能已可被对话触发（用户问健身/训练计划类问题时自动调用）。

## 用该技能构建安卓健身打卡 App（PWA）
- 用户要求用技能数据生成一个安卓健身打卡 App。环境无法编译原生 APK，故交付**移动端 PWA**（HTML/CSS/原生JS，可加到主屏离线使用，数据全本地 localStorage）。
- 产物目录：`fitness-app/`（index.html, css/style.css, js/{store,ai,app}.js, data/exercises.min.json, assets/videos/ 全部 1,324 GIF, manifest.webmanifest）。
- 四页：每日计划（AI 生成顺序动作 + 跟练计时/教练）、本月进度（日历+部位统计+月总结）、动作指导（区域模块+模糊/联想搜索）、身体情况（BMI 分析+照片本地留存+加强部位反馈计划）。
- AI 为本地启发式（数据集+用户指标），非云端 LLM，以保证离线。照片未做 CV 识别，改为手动标记加强部位。
- 本地静态服务：`python -m http.server 3000 --bind 127.0.0.1`（fitness-app 目录），预览 http://127.0.0.1:3000/。
- 修复的两个 bug：时长分段选择被默认值覆盖；跟练完成页会破坏二次跟练（改为叠加 overlay）。
- 验证：所有 JS node --check 通过；AI 逻辑 node 实测（搜索/计划/卡路里/BMI）正常；http 各资源 200。

## 把 PWA 做成真正 APK（Capacitor 原生工程）
- 用户要求「帮我做成APK」。环境探测：无 JDK / 无 Android SDK / 无 Gradle，但 npm registry 可达、app 用相对路径（WebView 安全）。
- 用 Capacitor v7 把 `fitness-app/` 包成原生 Android 工程：`npm i @capacitor/cli @capacitor/core @capacitor/android` → `npx cap add android`。
- 修复 cap 生成时的包名错配（java 文件在 com/fitcheck/app 但 package 声明仍是 com.getcapacitor.myapp）与缺失的 `colors.xml`：
  - 新建 `res/values/colors.xml`（colorPrimary #12b76a 等）。
  - MainActivity package、build.gradle namespace/applicationId 改为 `com.fitcheck.app`；strings.xml app_name=健身打卡。
  - 移除 INTERNET 权限（纯本地/离线）。
  - 品牌图标：用 Pillow 生成各密度 launcher（绿→蓝渐变+哑铃）+ 重绘 adaptive 矢量 + 替换 11 个 splash.png（gen_icons.py）。
- `npx cap sync android` 把 Web 资源（含 128MB GIF）拷入 `android/app/src/main/assets/public`。
- 卡点已确认：`npx cap build android` 因 `JAVA_HOME is not set and no 'java'` 失败——本沙箱无法编译二进制 APK。
- 交付两件让用户在本地/云端编译的东西：
  - `BUILD.md`：方式一 Android Studio 本地（Generate Signed APK）；方式二 GitHub Actions 云端。
  - `.github/workflows/build-apk.yml`：setup-java17 + android-actions/setup-android + `npx cap sync` + `./gradlew assembleDebug` + 上传 app-debug.apk 产物。
- 注：`cap add/sync` 在本环境会报 genie-safe-delete(trash) 错误，但工程仍正确生成；`cordova.js`/`cordova_plugins.js` 是 Capacitor 标准资源，无害。
- 改完 Web 后重新同步命令：`npx cap sync android`。

## 选定出包方式：GitHub Actions（最省事）
- 用户问「那个更方便走那个」，选了**GitHub Actions 云端出包**（零安装：不用装 Android Studio/JDK/SDK，CI 云端自带工具链）。
- 已 `git init` + 写 `.gitignore`（忽略 node_modules、android 构建中间产物、及 `android/app/src/main/assets/public/` 这类由 `npx cap sync android` 重新生成的目录；**保留** `fitness-app/assets/videos/*.gif` 源资源）。
- 已提交初始 commit（e67f938），含 android 工程 54 个源文件 + 1324 GIF + package-lock.json + gradle wrapper jar，仓库 ~115MB。
- 我无法代 push（GitHub 是用户账号），交付给用户的唯一动作：建 GitHub 仓库 → `git remote add origin ...` → `git push -u origin main` → Actions 里点 run → 下载 app-debug.apk（debug 包可直接装手机）。
- 提醒：debug APK 装手机需在设置开启「允许安装未知应用」；如需上架/正式签名 release，可改 workflow 用 keystore secret 出 signed release。

## 推送并触发 Actions 编译（成功）
- 用户仓库 `lionellee98/fitcheck-android` 已建。首次 push 因本机无凭据 401 失败；用户给 PAT（仅 repo 权限）→ GitHub 又拒：PAT 推 workflow 文件需额外 `workflow` scope。
- 用户重发带 `workflow` scope 的新 PAT → push 成功（`[new branch] main -> main`，commit e67f938）。
- 经验：用 PAT 推 GitHub Actions workflow，token 必须勾 `workflow` 权限，光 `repo` 不够。
- push 后立即用 `git remote set-url` 把 token 从远程 URL 抹掉（不落盘明文）。
- 通过 GitHub API 确认 Actions run `30235076948` 状态 IN_PROGRESS，自动触发编译。
- 后续用户自行在 Actions 页面下载 artifact `fitness-checkin-apk`（app-debug.apk）装手机。已建议编译完删除这两个临时 token。

## 每日计划页功能增强 + Radium 视觉升级（uiwrite 风格）
- 用户要求：每日计划页新增「训练部位」模块（AI 推荐 / 自选肌群多选）+ 场景严格动作匹配 + 整体高级感。
- 场景严格白名单（ai.js `ENV_EQUIPMENT`）：gym=null(全开)、home=['dumbbell','body weight']、outdoor=['body weight']。
  - 依据：数据集 equipment 分布为 body weight 325 / dumbbell 294 / cable 157 / barbell 154 / leverage machine 81 / smith 48 …；单杠/双杠/徒手均标 `body weight`。
  - 已 node 实测：gym 含 barbell/smith、home 仅 dumbbell+bodyweight、outdoor 仅 bodyweight，illegal=0，全部通过。
- generatePlan 新增 `regions` 显式入参（自选部位优先，否则 pickSplit AI 推荐）；热身/放松按场景约束器械。
- 前端：index.html 环境下方加 `partsModeSeg`(AI/手动) + `partsChips` + `partsHint` + `#planPreview`；app.js 加 `renderPlanPreview` 实时预览（随 env/时长/部位切换即刷新，含 scene-badge 场景徽章），`renderPartsChips` 多选。
- 视觉（style.css）：改 Radium 风格——氛围渐变背景(双 radial glow)、卡片 22px/按钮 999px pill、grad-soft 玻璃、柔和彩色阴影、premium hover(translateY+scale cubic-bezier(0.22,1,0.36,1))，保留明暗主题。
- 已 commit d097a71 并 push（用上次 PAT，仍有效），触发新 build run `30238651159`。沙箱内 HTTP 预览连不上(127.0.0.1 网络命名空间隔离)，不影响 Actions 出包。
- 临时脚本 analyze_eq.js/test_scenes.js/gen_data.py/gen_icons.py 已清理。
